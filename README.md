# Grep-JSON-GSON-
A modular CLI toolkit for Ubuntu/Linux. Includes a To-Do manager that outputs structured data and a "smart" generic filter that uses map-based detection to process any JSON stream via pipes.
